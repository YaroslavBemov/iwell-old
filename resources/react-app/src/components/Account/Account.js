import React from 'react';
import './Account.scss';

const Account = ({ children }) => {
    return ( 
        <div>
            Account
            { children }
        </div>
     );
}
 
export default Account;